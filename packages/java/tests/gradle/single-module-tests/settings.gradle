pluginManagement {
  repositories {
      mavenLocal()
      maven { url = 'https://maven.vaadin.com/vaadin-prereleases' }
      gradlePluginPortal()
  }
  plugins {
      id 'dev.hilla' version "${hillaVersion}"
  }
}
