name: Setup
description: Common setup workflow steps

inputs:
  node-version:
    description: Node.js version
    required: true
    default: 'lts/*'
  java-version:
    description: Java version
    required: true
    default: '17'
  cache-version:
    description: Cache version prefix — increment to bust all stale caches
    required: false
    default: 'v1'
  cache-maven:
    description: Whether to cache the Maven repository (~/.m2/repository). Disable for jobs that build from source to avoid stale SNAPSHOT artifacts.
    required: false
    default: 'true'

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: npm
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: ${{ inputs.java-version }}
        distribution: 'temurin'
    - name: Cache Maven repository
      if: ${{ inputs.cache-maven == 'true' }}
      uses: actions/cache@v4
      with:
        path: ~/.m2/repository
        key: ${{ inputs.cache-version }}-${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
    - name: Cache node_modules
      uses: actions/cache@v4
      id: cache-node_modules
      with:
        path: |
          node_modules
          packages/ts/*/node_modules
        key: ${{ inputs.cache-version }}-${{ runner.os }}-node_modules-${{ hashFiles('package-lock.json') }}
    - name: Install npm dependencies
      if: ${{ steps.cache-node_modules.outputs.cache-hit != 'true' }}
      shell: bash
      run: npm ci --ignore-scripts
